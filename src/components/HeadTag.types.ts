export interface HeadTagProps {
    title?: string;
    description?: string;
    canonical?: string;
    ogTitle?: string;
    ogType?: string;
    ogImage?: string;
    children?: React.ReactNode;
}
